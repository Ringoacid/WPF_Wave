<UserControl
    x:Class="WPF_Wave.UserControls.MultiSignalWaveformViewer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    SnapsToDevicePixels="True"
    UseLayoutRounding="True"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!--  Left labels area (draggable to reorder)  -->
        <ScrollViewer
            x:Name="LeftScrollViewer"
            Grid.Column="0"
            HorizontalScrollBarVisibility="Disabled"
            VerticalScrollBarVisibility="Disabled">
            <Canvas x:Name="LeftCanvas" />
        </ScrollViewer>

        <!--  Right waveform drawing area (horizontal scroll/zoom)  -->
        <ScrollViewer
            x:Name="WaveScrollViewer"
            Grid.Column="1"
            HorizontalScrollBarVisibility="Auto"
            ScrollChanged="WaveScrollViewer_ScrollChanged"
            SizeChanged="WaveScrollViewer_SizeChanged"
            VerticalScrollBarVisibility="Disabled">
            <Grid x:Name="WaveOuterGrid" HorizontalAlignment="Left">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <!-- Time axis -->
                <Canvas x:Name="TimeAxisCanvas" Grid.Row="0" Height="30" />
                <!-- Actual wave drawing surface -->
                <Grid
                    x:Name="WaveDrawGrid"
                    Grid.Row="1"
                    Width="100"
                    HorizontalAlignment="Left">
                    <Image
                        x:Name="WaveFromImage"
                        HorizontalAlignment="Left"
                        RenderOptions.BitmapScalingMode="NearestNeighbor"
                        Stretch="None" />
                    <Canvas x:Name="SignalValueDrawer" HorizontalAlignment="Left" />
                </Grid>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
