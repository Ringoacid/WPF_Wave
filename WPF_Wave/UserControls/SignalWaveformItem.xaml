<UserControl
    x:Class="WPF_Wave.UserControls.SignalWaveformItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:WPF_Wave.UserControls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DataContext="{d:DesignInstance local:SignalWaveformItem,
                                     IsDesignTimeCreatable=True}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    PreviewMouseWheel="UserControl_PreviewMouseWheel"
    SnapsToDevicePixels="True"
    UseLayoutRounding="True"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!--  信号名と、種類およびビット幅を表示する部分  -->
        <Border
            x:Name="SignalNameAndTypeBorder"
            Padding="5"
            HorizontalAlignment="Left"
            Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
            CornerRadius="5">
            <StackPanel Orientation="Vertical">
                <TextBlock
                    x:Name="SignalNameTextBlock"
                    FontSize="24"
                    FontWeight="Bold"
                    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                    TextWrapping="Wrap" />
                <TextBlock
                    x:Name="SignalTypeTextBlock"
                    FontSize="12"
                    Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                    TextWrapping="Wrap" />
            </StackPanel>
        </Border>

        <!--  波形表示用の部分  -->
        <ScrollViewer Grid.Column="1" HorizontalScrollBarVisibility="Auto">
            <Grid
                x:Name="WaveDrawGrid"
                MinWidth="100"
                HorizontalAlignment="Left">
                <!--  波形表示用のImage  -->
                <Image
                    x:Name="WaveFromImage"
                    HorizontalAlignment="Left"
                    RenderOptions.BitmapScalingMode="NearestNeighbor"
                    SnapsToDevicePixels="True"
                    Stretch="None" />

                <!--  多ビット信号の値を表示するTextBlockを持つCanvas  -->
                <Canvas x:Name="SignalValueDrawer" HorizontalAlignment="Left" />
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
